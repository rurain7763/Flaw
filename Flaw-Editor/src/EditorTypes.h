#pragma once

namespace flaw {
	enum class EditorTheme {
		Orange,
		Dark,
		Light,
		GrayOrange
	};

	enum class SceneState {
		Edit,
		Play,
	};

	enum class EditorMode {
		Selection,
		Landscape,
	};
}